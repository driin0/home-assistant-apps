name: "Iliad Tools"
description: "Portale di amministrazione per iliadbox (router Iliad Italia, Freebox OS)"
version: "1.0.0"
slug: "iliad_tools"
init: false

panel_icon: mdi:router-wireless
panel_title: "Iliad Tools"

ingress: true
ingress_port: 31996

url: "https://github.com/driin0/home-assistant-apps/tree/main/iliad-tools"
image: "ghcr.io/driin0/iliad-tools-app"

arch:
  - aarch64
  - amd64

startup: services
boot: manual

ports:
  31996/tcp: null

ports_description:
  31996/tcp: "Portale iliad-tools (accesso diretto, opzionale)"

options:
  port: 31996

schema:
  port: int?

map:
  - type: addon_config
    read_only: false
    path: /config
